import { OnModuleInit } from '@nestjs/common';
import { ConfigService } from '@nestjs/config';
import { Brand, BrandCategory, SupplierBrandRelation, SupplierSuggestion, BrandAnalysisResult, AutoSendConfig } from './brand.interface';
export declare class BrandIntelligenceService implements OnModuleInit {
    private configService;
    private readonly logger;
    private readonly dataFilePath;
    private database;
    private brandIndex;
    private brandAliasIndex;
    private supplierBrandIndex;
    private brandSupplierIndex;
    constructor(configService: ConfigService);
    onModuleInit(): Promise<void>;
    private loadDatabase;
    private initializeDatabase;
    private rebuildIndexes;
    private addCommonAliases;
    private normalizeName;
    private saveDatabase;
    detectBrands(text: string): string[];
    analyzeRequest(items: Array<{
        description: string;
        partNumber?: string;
        brand?: string;
    }>, additionalText?: string): BrandAnalysisResult;
    getSuggestedSuppliers(brands: string[]): SupplierSuggestion[];
    private categorizeSuppliers;
    private getRelation;
    recordSupplierResponse(supplierEmail: string, supplierName: string | undefined, brands: string[], isQuote: boolean, hasPrice?: boolean): Promise<void>;
    addBrand(name: string, category?: string, source?: 'auto_detected' | 'manual'): Promise<Brand>;
    addNewBrands(brandNames: string[], category?: string): Promise<Brand[]>;
    updateBrandCategory(brandName: string, newCategory: string): Promise<boolean>;
    findBrand(name: string): Brand | undefined;
    getBrandsByCategory(categoryKey: string): Brand[];
    getStatistics(): any;
    getAutoSendConfig(): AutoSendConfig;
    updateAutoSendConfig(config: Partial<AutoSendConfig>): Promise<AutoSendConfig>;
    getCategories(): BrandCategory[];
    searchBrands(query: string, limit?: number): Brand[];
    getSuppliersByBrand(brandName: string): SupplierBrandRelation[];
    getBrandsBySupplier(email: string): SupplierBrandRelation[];
}
